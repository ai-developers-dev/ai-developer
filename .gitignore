node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.mcp.json
.cta.json
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Local tooling config and scratch artifacts (contains .env files — never commit)
.codex/
artifacts/
